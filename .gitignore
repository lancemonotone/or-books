# Audit presentation — large media (link via app/scripts/link-media.* or copy on deploy)
app/media/*
!app/media/.gitkeep

# Optional local API config (repo root — sibling to app/)
/config.php

# Local Node deps for one-shot scripts
app/node_modules/