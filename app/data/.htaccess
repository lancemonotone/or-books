# Keep presentation YAML/JSON fresh after editor saves (delete, edit, create).
<IfModule mod_headers.c>
  Header set Cache-Control "no-store, no-cache, must-revalidate"
  Header set Pragma "no-cache"
</IfModule>
